console.log('The data type of the outermost layer (Number, String, Object, Array or else) is an object');

console.log('The hits property is where the job results are stored, is it a Number, String, Array, Object and hits is an array');

console.log(`We can use a for loop to iterate through the hits array and get access to required properties in each element using object destructuring`);